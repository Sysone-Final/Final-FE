export interface RackDevice {
  id: number;
  name: string;
  position: number;
  height: number;
}

export interface DeviceCard {
  key: string;
  label: string;
  size: string;
  img: string;
  borderColor: string;
}
